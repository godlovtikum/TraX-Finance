# This file is generated by `react-native setup-ios-framework` (or equivalent).
# It is used by the Xcode build phase "Bundle React Native code and images".
# NODE_BINARY specifies the path to the node executable used during the build.

# Uncomment and set the absolute path to your node binary if the build fails:
# export NODE_BINARY=/usr/local/bin/node
export NODE_BINARY=$(command -v node)
